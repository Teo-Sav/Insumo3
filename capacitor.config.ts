import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Insumo3Aporte3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
